import HCheckboxButton from '../checkbox/src/checkbox-button.vue'

/* istanbul ignore next */
HCheckboxButton.install = function(Vue) {
  Vue.component(HCheckboxButton.name, HCheckboxButton)
}

export default HCheckboxButton
