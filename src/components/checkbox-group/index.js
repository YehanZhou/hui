import HCheckboxGroup from '../checkbox/src/checkbox-group.vue'

/* istanbul ignore next */
HCheckboxGroup.install = function(Vue) {
  Vue.component(HCheckboxGroup.name, HCheckboxGroup)
}

export default HCheckboxGroup
